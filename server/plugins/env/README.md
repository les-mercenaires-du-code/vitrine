# Env

config object accept three params

```
const config = {
  devPath: '../.env_dev',
  prodPath: '../.env_prod',
  testPath: '../.env_test',
}
```

all path will be resolved depending on callee path
